# Project 08 context

Use [AGENTS.md](AGENTS.md) for shared instructions and [Implementation.md](Implementation.md) for the authoritative ledger. This is a planning-only fallback folder, not a configured worktree. Do not generate serving code/config until an implementation phase is authorized. Follow the same benchmark, privacy and review gates as every coding agent. No separate Claude-specific workflow or duplicate status ledger is needed.
