# Project 08 — Local Inference Server planning package

Start with [Implementation.md](Implementation.md). This independently readable package plans a private vLLM service, measured KV/prefix-cache behavior, weight quantization and optional supported KV quantization. It contains no implementation code, runtime configuration or measured results. Repository/worktree setup remains pending because no suitable project repository was identified in the bounded inventory.

Morning review: confirm GPU/budget and license, private single-host boundary, provisional service/quality targets and three proposed ADRs. Then authorize Phase 00 only. Zero provider API fees does not mean zero compute cost.
